// Remotion entry point. Run:
//   npm run video:preview   — opens Remotion Studio in the browser
//   npm run video:render    — renders to /public/videos/finals-hype.mp4

import { registerRoot } from "remotion";
import { Root } from "./Root";

registerRoot(Root);
